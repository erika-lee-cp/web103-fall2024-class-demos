{
  "name": "web103-unit9-demo",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"cd client && vite\" \"cd server && nodemon server\"",
    "start": "cd server && npm install && npm start",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "pg": "^8.10.0"
  }
}
